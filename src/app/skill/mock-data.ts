// Define interface for Employee
export interface Employee {
  user_id: string;
  id: number;
  skill_name: string;
  YOE: string;
  rating: string;
}

// Sample employees
export const EMPLOYEES: Employee[] = [];
